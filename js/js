const bgm = document.getElementById("music.js.mp3");
const btn = document.getElementById("music-toggle");

let playing = false;

btn.onclick = () => {
  if (!playing) {
    bgm.volume = 0.3;
    bgm.play();
    btn.textContent = "⏸";
  } else {
    bgm.pause();
    btn.textContent = "🎵";
  }
  playing = !playing;
};
